# 作業系統基本概念與名詞整理

本文件簡要介紹常見的作業系統 (Operating System) 名詞與核心概念，並整理常見的 NVIDIA (NV) 面試相關題目與參考解答，方便快速複習。

## 1. 基本名詞與概念

- **核心 (Kernel)**：作業系統的核心組件，負責管理硬體資源並提供系統呼叫介面。
- **程序 (Process)**：程式執行時的實體，每個程序擁有獨立的記憶體空間與系統資源。
- **執行緒 (Thread)**：程序內的執行單元，共享程序的記憶體與資源，可實現並行運算。
- **行程切換 (Context Switch)**：CPU 從一個程序或執行緒切換到另一個的過程，涉及暫存器與狀態保存/恢復。
- **記憶體管理**：包含虛擬記憶體 (Virtual Memory)、分頁 (Paging) 與分段 (Segmentation) 等機制，用來提升效率與安全性。
- **檔案系統 (File System)**：負責檔案與資料夾的儲存與管理，如 NTFS、EXT4 等。
- **排程 (Scheduling)**：決定程序或執行緒在 CPU 上的執行順序，常見演算法有 FCFS、RR、優先權排程等。
- **同步與互斥**：在多執行緒環境中，使用 Mutex、Semaphore 或 Spinlock 等機制避免資源競爭。

## 2. NVIDIA 面試常見題目

以下列出幾個與作業系統相關的 NVIDIA 面試題目範例及參考解答，僅供準備時參考。

### 題目 1：程序與執行緒的差異是什麼？
**解答**：程序是作業系統分配資源的基本單位，每個程序擁有獨立的記憶體空間；執行緒則是在程序內執行的最小單位，多個執行緒共享程序的記憶體與資源。使用多執行緒可提升效能，但需注意同步與互斥問題。

### 題目 2：請解釋虛擬記憶體的概念及其優點。
**解答**：虛擬記憶體將實體記憶體與儲存裝置結合，提供比實體記憶體更大的可用空間，並透過分頁或分段等機制實現。其優點包括：
1. 讓程序看到連續的記憶體區域，簡化開發。
2. 提供記憶體保護與隔離，提高穩定性與安全性。
3. 允許部分程式碼或資料暫時存放在磁碟，增加可同時執行的程序數量。

### 題目 3：什麼是行程 (或執行緒) 切換？切換時需要保存哪些資訊？
**解答**：行程切換是指 CPU 從執行一個程序或執行緒轉而執行另一個。切換時需保存當前程序/執行緒的暫存器內容、程式計數器、堆疊指標等狀態，並載入下一個程序/執行緒的狀態。切換頻繁會造成額外的耗時 (context switch overhead)。

### 題目 4：在多核心處理器上，如何有效同步多個執行緒？
**解答**：常見方式包括使用 Mutex、Semaphore、Spinlock 或以原子操作 (atomic operation) 方式實現的鎖。選擇適當的同步機制需考量臨界區長度、等待時間與效能影響。Spinlock 適用於短暫鎖定，Mutex 適合較長時間的臨界區。

### 題目 5：描述一個你在專案中排除競爭條件 (Race Condition) 的經驗。
**解答**：可舉例說明在多執行緒環境中使用鎖或條件變數解決競爭問題，例如利用互斥鎖保護共享資源，或採用條件變數協調執行緒間的順序，以確保資料一致性。

以上內容可作為快速複習的材料，實際面試時請根據自身經驗與理解深入說明。
